function sequence(params) {
    
    let character = String(params[0]);

    for (let index = 0; index < character.length; index++) {
      console.log(character[index]);
        
    }
}
sequence(["softuni"]);